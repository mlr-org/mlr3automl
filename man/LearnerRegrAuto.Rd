% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/LearnerRegrAuto.R
\name{LearnerRegrAuto}
\alias{LearnerRegrAuto}
\title{Regression AutoML Learner}
\value{
Object of class \link[R6:R6Class]{R6::R6Class} and \code{LearnerRegrAuto}.
}
\description{
The \link{LearnerRegrAuto} is an automated machine learning (AutoML) system for regression tasks.
It combines preprocessing, a switch between multiple learners,
and hyperparameter tuning to find the best model for the given task.
}
\section{Debugging}{

Set \code{options(bbotk.debug = TRUE)} to run the tuning in the main session.
Set \code{encapsulate_learner = FALSE} to remove encapsulation of the learner.
Set \code{encapsulate_mbo = FALSE} to catch no errors in mbo.
}

\section{Parameters}{

\describe{
\item{bagging}{(\code{logical(1)})\cr
Whether to train each configuration as a bagged ensemble.
A configuration trains \code{bagging_folds} child models on a cross-validation of the training data,
and the test prediction is the equally weighted average of the children.
Tasks with fewer than \code{bagging_small_size} rows are instead cross-validated with
\code{bagging_small_folds} folds repeated \code{bagging_small_repeats} times.
The configuration is scored by its out-of-fold predictions,
so the tuning archive reports the scores in the \code{internal_valid_score} column and
the \code{resampling} and \code{small_data_resampling} parameters have no effect.
The neural network learners (mlp, resnet, ft_transformer, and fastai) and tabpfn instead fit a
single final model on the complete training data after the cross-validation.
The final model reuses the exact model of the winning configuration trained during tuning.
Default is \code{TRUE}.}

\item{bagging_folds}{(\code{integer(1)})\cr
Number of cross-validation folds and therefore child models of a bagged configuration.
Default is \code{8}.}

\item{bagging_small_folds}{(\code{integer(1)})\cr
Number of cross-validation folds of a bagged configuration on a small data set.
Default is \code{5}.}

\item{bagging_small_repeats}{(\code{integer(1)})\cr
Number of repetitions of the cross-validation of a bagged configuration on a small data set.
A bagged configuration therefore trains \code{bagging_small_folds * bagging_small_repeats} child models.
Default is \code{5}.}

\item{bagging_small_size}{(\code{integer(1)})\cr
Number of rows up to which a task counts as a small data set for \code{bagging}.
Default is \code{500}.}

\item{learner_timeout}{(\code{integer(1)})\cr
Timeout for training and predicting with a single configuration.
With \code{bagging}, each child model receives an equal share of the timeout.}

\item{n_threads}{(\code{integer(1)})\cr
Number of threads used for training a single learner.}

\item{n_cpu}{(named \code{integer()})\cr
Number of CPUs a single training of a learner uses, named by learner id, e.g. \code{c(xgboost = 1)}.
Overrides the default of the learner.
Must be at least 1 because the worker always runs on the CPU.
Currently informational only; the number of threads is controlled by \code{n_threads}.}

\item{n_gpu}{(named \code{integer()})\cr
Number of GPUs a single training of a learner uses, named by learner id, e.g. \code{c(xgboost = 1)}.
Overrides the default of the learner.
Can only be 0 or 1 for now.
The torch learners, TabPFN, TabFM, and fastai default to 1; all other learners default to 0.
Only effective when \code{"cuda"} is part of \code{devices}; otherwise every learner is trained on the CPU.
Every learner is a subspace of the search space that is tuned with \link[mlr3mbo:TunerADBOThompson]{mlr3mbo::TunerADBOThompson},
and every subspace runs on a \CRANpkg{mirai} compute profile.
Without compute profiles or with a single compute profile, all learners run on the same workers.
When the daemons of the compute profiles \code{"mlr3automl_cpu"} and \code{"mlr3automl_gpu"} are set up with
\code{\link[rush:rush_plan]{rush::rush_plan()}}, the learners with a \code{n_gpu} requirement of 1 run on the gpu profile and the remaining
learners on the cpu profile.
The workers of a profile only ever propose and evaluate points of the learners of that profile,
and a profile that runs no learner stays idle.
Other combinations of compute profiles are not supported.

\if{html}{\out{<div class="sourceCode">}}\preformatted{mirai::daemons(7, .compute = "mlr3automl_cpu")
mirai::daemons(1, .compute = "mlr3automl_gpu")
rush::rush_plan(profiles = c(mlr3automl_cpu = 7, mlr3automl_gpu = 1))
}\if{html}{\out{</div>}}

}

\item{memory_limit}{(\code{integer(1)})\cr
Memory limit for training a single learner in MB.
The limit is shared across the parallel workers, i.e. divided by the number of workers.}

\item{devices}{(\code{character()})\cr
Devices to use for model training.
Possible values are \code{"cpu"} and \code{"cuda"}.
If \code{"cuda"}, learners with a \code{n_gpu} requirement of 1 are trained on a GPU,
while the remaining learners stay on the CPU.}

\item{large_data_size}{(\code{integer(1)})\cr
Threshold for the data set size (number of rows times number of columns) above
which large-data rules apply.
Beyond this threshold the number of parallel workers is reduced to a quarter, rounded up, and each
remaining worker is given proportionally more threads and memory.
When the workers are distributed over \CRANpkg{mirai} compute profiles, the number of workers of every
profile is reduced, but every profile keeps at least one worker.
The \code{"mlr3automl_gpu"} profile is exempt because its number of workers is fixed by the number of GPUs.
It keeps its workers, threads, and memory limit so that the gpu learners do not claim the CPU cores and
the memory that are freed on the cpu profiles.}

\item{small_data_size}{(\code{integer(1)})\cr
Threshold value for the data set size from which special rules apply.}

\item{small_data_resampling}{(\link[mlr3:Resampling]{mlr3::Resampling})\cr
Resampling strategy to use for model training on small data sets.
Only used when \code{bagging} is \code{FALSE}.}

\item{initial_design_default}{(\code{logical(1)})\cr
Whether to use the default design of the learner.}

\item{initial_design_set}{(\code{integer(1)})\cr
Number of points to use for the initial design set.}

\item{initial_design_size}{(\code{integer(1)})\cr
Size of the random, sobol or lhs initial design.}

\item{initial_design_type}{(\code{character(1)})\cr
Type of the initial design used for mbo.
Possible values are \code{"lhs"}, \code{"sobol"}, \code{"random"}.
\code{"lhs"} uses a Latin Hypercube Sampling design.
\code{"sobol"} uses a Sobol sequence design.
\code{"random"} uses a random design.}

\item{initial_design_fraction}{(\code{numeric(1)})\cr
Fraction of the budget to use for the initial design.
The remaining points of the designs of all learners are dropped, because every compute profile has its own
queue.}

\item{resampling}{(\link[mlr3:Resampling]{mlr3::Resampling})\cr
Resampling strategy used for tuning.
Only used when \code{bagging} is \code{FALSE}.}

\item{terminator}{(\link[bbotk:Terminator]{bbotk::Terminator})\cr
Terminator criterion for tuning.}

\item{measure}{(\link[mlr3:Measure]{mlr3::Measure})\cr
Measure used for tuning.}

\item{callbacks}{(\link[mlr3tuning:CallbackAsyncTuning]{mlr3tuning::CallbackAsyncTuning})\cr
Callbacks used for tuning.}

\item{store_benchmark_result}{(\code{logical(1)})\cr
Whether to store the benchmark result.}

\item{store_models}{(\code{logical(1)})\cr
Whether to store the models.}

\item{encapsulate_learner}{(\code{logical(1)})\cr
Whether to encapsulate the learner.
Change to \code{FALSE} to debug.}

\item{encapsulate_mbo}{(\code{logical(1)})\cr
Whether to encapsulate the tuning.
Change to \code{FALSE} to debug.}

\item{check_learners}{(\code{logical(1)})\cr
Whether to check if the learners are compatible with the task.
Change to \code{FALSE} to debug.}

}
}

\section{Python learners}{

Python learners like \code{TabPFN}, \code{TabFM}, and \code{fastai} run via \code{reticulate} and
therefore need a Python installation with their required packages.
There are two ways to provide it:
\enumerate{
\item Do nothing and let \code{reticulate::py_require()} install the required packages into an ephemeral virtual
environment automatically.
\item Point the \code{RETICULATE_PYTHON} environment variable to a Python installation that has the required packages
installed.
}

We recommend option 2 when running on many workers,
as it avoids the overhead of downloading and installing the packages on each worker.
Use \code{\link[=install_python_learners]{install_python_learners()}} to create a conda environment with the required packages and set
\code{RETICULATE_PYTHON} to the returned Python binary.

The \code{TabPFN} learner additionally requires the \code{TABPFN_TOKEN} environment variable to download the model weights.
}

\examples{
learner = lrn("regr.auto")
learner
}
\section{Super classes}{
\code{\link[mlr3:Learner]{mlr3::Learner}} -> \code{\link[mlr3automl:LearnerAuto]{LearnerAuto}} -> \code{LearnerRegrAuto}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-LearnerRegrAuto-initialize}{\code{LearnerRegrAuto$new()}}
    \item \href{#method-LearnerRegrAuto-clone}{\code{LearnerRegrAuto$clone()}}
  }
}
\if{html}{\out{<details><summary>Inherited methods</summary>
<ul>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="base_learner"><a href='../../mlr3/html/Learner.html#method-Learner-base_learner'><code>mlr3::Learner$base_learner()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="configure"><a href='../../mlr3/html/Learner.html#method-Learner-configure'><code>mlr3::Learner$configure()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="format"><a href='../../mlr3/html/Learner.html#method-Learner-format'><code>mlr3::Learner$format()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="help"><a href='../../mlr3/html/Learner.html#method-Learner-help'><code>mlr3::Learner$help()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="predict"><a href='../../mlr3/html/Learner.html#method-Learner-predict'><code>mlr3::Learner$predict()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="predict_newdata"><a href='../../mlr3/html/Learner.html#method-Learner-predict_newdata'><code>mlr3::Learner$predict_newdata()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="print"><a href='../../mlr3/html/Learner.html#method-Learner-print'><code>mlr3::Learner$print()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="reset"><a href='../../mlr3/html/Learner.html#method-Learner-reset'><code>mlr3::Learner$reset()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="selected_features"><a href='../../mlr3/html/Learner.html#method-Learner-selected_features'><code>mlr3::Learner$selected_features()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3" data-topic="Learner" data-id="train"><a href='../../mlr3/html/Learner.html#method-Learner-train'><code>mlr3::Learner$train()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3automl" data-topic="LearnerAuto" data-id="encapsulate"><a href='../../mlr3automl/html/LearnerAuto.html#method-LearnerAuto-encapsulate'><code>LearnerAuto$encapsulate()</code></a></span></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-LearnerRegrAuto-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-LearnerRegrAuto-initialize}{}}}
\subsection{\code{LearnerRegrAuto$new()}}{
  Creates a new instance of this \link[R6:R6Class]{R6} class.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{LearnerRegrAuto$new(id = "regr.auto", learner_ids, rush = NULL)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{id}}{(\code{character(1)})\cr
Identifier for the new instance.}
      \item{\code{learner_ids}}{(\code{character()})\cr
Learner that should be used.}
      \item{\code{rush}}{\link[rush:Rush]{rush::Rush}\cr
Rush instance.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-LearnerRegrAuto-clone"></a>}}
\if{latex}{\out{\hypertarget{method-LearnerRegrAuto-clone}{}}}
\subsection{\code{LearnerRegrAuto$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{LearnerRegrAuto$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
