% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PipeOpLearnerBagged.R
\name{PipeOpLearnerBagged}
\alias{PipeOpLearnerBagged}
\title{Bagged Learner PipeOp}
\value{
Object of class \link[R6:R6Class]{R6::R6Class} and \code{PipeOpLearnerBagged}.
}
\description{
Wraps a learner graph into a bagged ensemble in the style of AutoGluon and TabArena.
During training, the graph is fitted on \code{folds} cross-validation folds of the training data,
so each child model is trained on all but one fold.
With \code{repeats} larger than \code{1}, the cross-validation is repeated and the ensemble holds \code{folds * repeats}
child models.
The out-of-fold predictions are scored with \code{measure} and the score is reported as \verb{$internal_valid_scores}.
During prediction, the child models predict on the new data and their predictions are combined with equal weights.
Probabilities are averaged, responses are aggregated by majority vote, and regression responses are averaged.

If the terminal learner supports validation, each child uses its held-out fold as validation set,
so internally tuned parameters (e.g., early-stopped boosting rounds) are tuned per child.
The tuned values are aggregated across the children with the aggregation rules of \code{internal_search_space}
and reported as \verb{$internal_tuned_values}.

When \code{refit} is set, a single model is trained on the complete data instead of the ensemble,
and neither an out-of-fold score nor internally tuned values are produced.
This is meant for the final model fit of learners whose ensemble is too expensive to deploy,
after the tuned values of the ensemble have been set from the outside.
}
\section{Parameters}{

The parameters of the terminal learner, as well as:
\itemize{
\item \code{bagging.folds} :: \code{integer(1)}\cr
Number of cross-validation folds. Initialized to \code{8}.
\item \code{bagging.repeats} :: \code{integer(1)}\cr
Number of repetitions of the cross-validation. Initialized to \code{1}.
\item \code{bagging.refit} :: \code{logical(1)}\cr
Whether to train a single model on the complete data instead of the cross-validated ensemble.
Initialized to \code{FALSE}.
}
}

\section{Super class}{
\code{\link[mlr3pipelines:PipeOp]{mlr3pipelines::PipeOp}} -> \code{PipeOpLearnerBagged}
}
\section{Active bindings}{
  \if{html}{\out{<div class="r6-active-bindings">}}
  \describe{
    \item{\code{validate}}{(\code{character(1)} or \code{NULL})\cr
Only \code{NULL} and \code{"predefined"} are accepted.
The pipeop creates its validation data internally from the cross-validation folds,
so an incoming validation task is discarded.}

    \item{\code{internal_valid_scores}}{(named \code{list()} or \code{NULL})\cr
Score of the out-of-fold predictions, named by the measure id.}

    \item{\code{internal_tuned_values}}{(named \code{list()} or \code{NULL})\cr
Internally tuned values aggregated across the child models.}

    \item{\code{predict_type}}{(\code{character(1)})\cr
Predict type of the wrapped graph learner.}

    \item{\code{learner}}{(\link[mlr3pipelines:GraphLearner]{mlr3pipelines::GraphLearner})\cr
Wrapped graph learner.}

    \item{\code{learner_model}}{(\link[mlr3pipelines:GraphLearner]{mlr3pipelines::GraphLearner} or \code{list()})\cr
If trained, the child models as list of graph learners.
Otherwise, the wrapped graph learner.}
  }
  \if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
  \itemize{
    \item \href{#method-PipeOpLearnerBagged-initialize}{\code{PipeOpLearnerBagged$new()}}
    \item \href{#method-PipeOpLearnerBagged-clone}{\code{PipeOpLearnerBagged$clone()}}
  }
}
\if{html}{\out{<details open><summary>Inherited methods</summary>
<ul>
  <li><span class="pkg-link" data-pkg="mlr3pipelines" data-topic="PipeOp" data-id="help"><a href='../../mlr3pipelines/html/PipeOp.html#method-PipeOp-help'><code>mlr3pipelines::PipeOp$help()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3pipelines" data-topic="PipeOp" data-id="predict"><a href='../../mlr3pipelines/html/PipeOp.html#method-PipeOp-predict'><code>mlr3pipelines::PipeOp$predict()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3pipelines" data-topic="PipeOp" data-id="print"><a href='../../mlr3pipelines/html/PipeOp.html#method-PipeOp-print'><code>mlr3pipelines::PipeOp$print()</code></a></span></li>
  <li><span class="pkg-link" data-pkg="mlr3pipelines" data-topic="PipeOp" data-id="train"><a href='../../mlr3pipelines/html/PipeOp.html#method-PipeOp-train'><code>mlr3pipelines::PipeOp$train()</code></a></span></li>
</ul>
</details>}}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PipeOpLearnerBagged-initialize"></a>}}
\if{latex}{\out{\hypertarget{method-PipeOpLearnerBagged-initialize}{}}}
\subsection{\code{PipeOpLearnerBagged$new()}}{
  Creates a new instance of this \link[R6:R6Class]{R6} class.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{PipeOpLearnerBagged$new(
  graph,
  id,
  measure,
  internal_search_space = NULL,
  param_vals = list()
)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{graph}}{(\link[mlr3pipelines:Graph]{mlr3pipelines::Graph})\cr
Learner graph to bag.
The terminal \link[mlr3pipelines:PipeOpLearner]{mlr3pipelines::PipeOpLearner} must have the id \code{id}.}
      \item{\code{id}}{(\code{character(1)})\cr
Identifier of the resulting object.}
      \item{\code{measure}}{(\link[mlr3:Measure]{mlr3::Measure})\cr
Measure to score the out-of-fold predictions with.}
      \item{\code{internal_search_space}}{(\link[paradox:ParamSet]{paradox::ParamSet})\cr
Internal search space with aggregation rules for the internally tuned parameters.
The ids must match the names of the \verb{$internal_tuned_values} of the wrapped graph learner.}
      \item{\code{param_vals}}{(named \code{list()})\cr
List of hyperparameter settings.}
    }
    \if{html}{\out{</div>}}
  }
}

\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-PipeOpLearnerBagged-clone"></a>}}
\if{latex}{\out{\hypertarget{method-PipeOpLearnerBagged-clone}{}}}
\subsection{\code{PipeOpLearnerBagged$clone()}}{
  The objects of this class are cloneable with this method.
  \subsection{Usage}{
    \if{html}{\out{<div class="r">}}
    \preformatted{PipeOpLearnerBagged$clone(deep = FALSE)}
    \if{html}{\out{</div>}}
  }
  \subsection{Arguments}{
    \if{html}{\out{<div class="arguments">}}
    \describe{
      \item{\code{deep}}{Whether to make a deep clone.}
    }
    \if{html}{\out{</div>}}
  }
}

}
